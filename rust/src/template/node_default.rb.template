class _CLASS_ < _BASE_
_TS_# Called when the node enters the scene tree for the first time.
_TS_def _ready
_TS_end

_TS_# Called every frame. 'delta' is the elapsed time since the previous frame.
_TS_def _process(delta)
_TS_end
end
