module Unit
  module Loader
    module Bin
      # Holds what Bolt names in a class body inside Bin, where Ruby looks
      # through the enclosing namespaces.
      class Tray
        HOLDS = Bolt
      end
    end
  end
end
