# Glossary

The words this project keeps, and the ones it turns down in their place.

## Godot mruby

### Includes

- `rust/src/**/*.rs`
- `tasks/**/*.rb`
- `tasks/*.rake`
- `Rakefile`
- `build_config/*.rb`
- `CLAUDE.md`
- `.spec/**/*.md`
